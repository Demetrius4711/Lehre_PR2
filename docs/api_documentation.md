# API-Dokumentation

## UserService

### get_user(user_id)
Holt einen User anhand der ID.

**Parameter:**
- `user_id` (int): Die ID des Users

**Rückgabe:**
- dict: User-Daten

### create_user(user_data)
Erstellt einen neuen User.

**Parameter:**
- `user_data` (dict): Die Daten des neuen Users

**Rückgabe:**
- dict: Die erstellten User-Daten
